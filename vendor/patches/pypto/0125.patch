From 1124d5c2562a7aaec4830d4dfde9ff5e686a6998 Mon Sep 17 00:00:00 2001
From: zhaosiying12138 <neucszhaopeng2016@163.com>
Date: Fri, 28 Aug 2026 01:50:10 +0800
Subject: [PATCH 125/263] Advance TensorIR rewrite epoch loads

---
 ...-lazy-loads-on-every-rewrite-attempt.patch | 77 +++++++++++++++++++
 3rdparty/nvidia/tensor-ir                     |  2 +-
 cmake/nvidia_backend.cmake                    |  2 +-
 3 files changed, 79 insertions(+), 2 deletions(-)
 create mode 100644 3rdparty/nvidia/0018-Invalidate-lazy-loads-on-every-rewrite-attempt.patch

diff --git a/3rdparty/nvidia/0018-Invalidate-lazy-loads-on-every-rewrite-attempt.patch b/3rdparty/nvidia/0018-Invalidate-lazy-loads-on-every-rewrite-attempt.patch
new file mode 100644
index 0000000000000000000000000000000000000000..ab990aca5ba0b8778583835457b849b92bf761c2
--- /dev/null
+++ b/3rdparty/nvidia/0018-Invalidate-lazy-loads-on-every-rewrite-attempt.patch
@@ -0,0 +1,77 @@
+From 76a49bb3db2922e3c6dd06c1218497fe8e9f9b92 Mon Sep 17 00:00:00 2001
+From: zhaosiying12138 <neucszhaopeng2016@163.com>
+Date: Fri, 28 Aug 2026 01:49:44 +0800
+Subject: [PATCH 18/18] Invalidate lazy loads on every rewrite attempt
+
+---
+ .../TensorToCudaTile/LayoutPropagationImpl.cpp | 18 ++++++++++--------
+ 1 file changed, 10 insertions(+), 8 deletions(-)
+
+diff --git a/lib/Conversion/TensorToCudaTile/LayoutPropagationImpl.cpp b/lib/Conversion/TensorToCudaTile/LayoutPropagationImpl.cpp
+index b7b259a..0ae39d8 100644
+--- a/lib/Conversion/TensorToCudaTile/LayoutPropagationImpl.cpp
++++ b/lib/Conversion/TensorToCudaTile/LayoutPropagationImpl.cpp
+@@ -10,6 +10,8 @@
+ 
+ #include "cuda_tile/Dialect/CudaTile/IR/Ops.h"
+ 
++#include <cstdint>
++
+ namespace tcg = mlir::nv_tensor_ir::tcutegen;
+ 
+ //===----------------------------------------------------------------------===//
+@@ -140,8 +142,8 @@ private:
+   /// materialized lazily from getTile so each new load immediately gains a
+   /// real SSA consumer instead of living only in this side table.
+   ConversionPatternRewriter *activeRewriter = nullptr;
+-  Operation *activeOperation = nullptr;
+-  DenseMap<IterationSpace *, Operation *> loadedTileCacheOwner;
++  uint64_t rewriteEpoch = 0;
++  DenseMap<IterationSpace *, uint64_t> loadedTileCacheEpoch;
+ 
+   /// A cached load is only safe while one conversion pattern is being
+   /// rewritten. Graph splitting may erase that pattern's replacement later,
+@@ -747,7 +749,7 @@ public:
+     state.blockStructure = &state.blockStructures[resultsOp.getOperation()];
+     state.iterationSpace = &state.blockStructure->iterationSpaces[0];
+     state.activeRewriter = &rewriter;
+-    state.activeOperation = resultsOp.getOperation();
++    ++state.rewriteEpoch;
+     state.layouts.clear();
+     for (Value operand : resultsOp.getOperands()) {
+       auto layout = dyn_cast_or_null<LayoutSourceAttrInterface>(
+@@ -931,8 +933,8 @@ LogicalResult ConversionStateImpl::start(GraphOp graphOp) {
+   blockStructure = nullptr;
+   iterationSpace = nullptr;
+   activeRewriter = nullptr;
+-  activeOperation = nullptr;
+-  loadedTileCacheOwner.clear();
++  rewriteEpoch = 0;
++  loadedTileCacheEpoch.clear();
+   layouts.clear();
+   operandIndex = 0;
+ 
+@@ -946,7 +948,7 @@ LogicalResult ConversionStateImpl::update(ConversionPatternRewriter &rewriter,
+                                           Operation *op) {
+   LLVM_DEBUG(llvm::dbgs() << "Converting: " << op->getName() << "\n");
+   activeRewriter = &rewriter;
+-  activeOperation = op;
++  ++rewriteEpoch;
+ 
+   // Verify the attribute-level legality (layout / iteration-space-id presence
+   // and validity). This is shared with the pre-flight feasibility check. Here,
+@@ -1041,9 +1043,9 @@ Value ConversionStateImpl::getTile(Value operand) {
+       iterationSpace = &blockStructure->iterationSpaces[idx];
+     }
+ 
+-    if (loadedTileCacheOwner.lookup(iterationSpace) != activeOperation) {
++    if (loadedTileCacheEpoch.lookup(iterationSpace) != rewriteEpoch) {
+       resetLoadedTileCache(*iterationSpace);
+-      loadedTileCacheOwner[iterationSpace] = activeOperation;
++      loadedTileCacheEpoch[iterationSpace] = rewriteEpoch;
+     }
+     auto it = iterationSpace->loadedTiles.find(tensorSource);
+     assert(it != iterationSpace->loadedTiles.end() &&
+-- 
+2.53.0
+
diff --git a/3rdparty/nvidia/tensor-ir b/3rdparty/nvidia/tensor-ir
index 4c3f0821f8ffe33ca1f6995e5c6943889a19e816..76a49bb3db2922e3c6dd06c1218497fe8e9f9b92 160000
--- a/3rdparty/nvidia/tensor-ir
+++ b/3rdparty/nvidia/tensor-ir
@@ -1 +1 @@
-Subproject commit 4c3f0821f8ffe33ca1f6995e5c6943889a19e816
+Subproject commit 76a49bb3db2922e3c6dd06c1218497fe8e9f9b92
diff --git a/cmake/nvidia_backend.cmake b/cmake/nvidia_backend.cmake
index c04ed953590671ab9fd6ed7c628e328c5ccd554a..3f21b2aefe03304703558d47769411eb24b83671 100644
--- a/cmake/nvidia_backend.cmake
+++ b/cmake/nvidia_backend.cmake
@@ -36,7 +36,7 @@ foreach(_required_source
 endforeach()
 
 set(PYPTO_NVIDIA_TENSOR_IR_REVISION
-    "4c3f0821f8ffe33ca1f6995e5c6943889a19e816")
+    "76a49bb3db2922e3c6dd06c1218497fe8e9f9b92")
 set(PYPTO_NVIDIA_CUDA_TILE_REVISION
     "af2417041cc939b87ef56d92cfdcf61737c5457e")
 set(PYPTO_NVIDIA_LLVM_REVISION
