From 50e16afb8d2dc9437fdd05db1e66df70ccd0e016 Mon Sep 17 00:00:00 2001
From: zhaosiying12138 <neucszhaopeng2016@163.com>
Date: Fri, 28 Aug 2026 12:15:06 +0800
Subject: [PATCH 186/212] Order final GDN output after state scatter

---
 src/codegen/nvidia/tensor_ir_codegen.cpp | 131 ++++++++---------------
 1 file changed, 47 insertions(+), 84 deletions(-)

diff --git a/src/codegen/nvidia/tensor_ir_codegen.cpp b/src/codegen/nvidia/tensor_ir_codegen.cpp
index d2aae5ba25ae57223b1e4434db5197347f6b24cc..2bfbd2e21f22549ce216f8377d0e9e3963244ff5 100644
--- a/src/codegen/nvidia/tensor_ir_codegen.cpp
+++ b/src/codegen/nvidia/tensor_ir_codegen.cpp
@@ -9131,11 +9131,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
   indices_full.dense_strides = MakeDenseStridesForMetadata(indices_full.shape);
   StaticTensor state_row{DataType::FP32, {1, state_width}, {}};
   state_row.dense_strides = MakeDenseStridesForMetadata(state_row.shape);
-  StaticTensor state_anchor_row{DataType::FP32, {1, value_width}, {}};
-  state_anchor_row.dense_strides =
-      MakeDenseStridesForMetadata(state_anchor_row.shape);
-  StaticTensor state_anchor_row_bf16 = state_anchor_row;
-  state_anchor_row_bf16.dtype = DataType::BF16;
   StaticTensor state_group{DataType::FP32,
                            {graph.q_heads, heads_per_query, graph.value_dim,
                             graph.key_dim}, {}};
@@ -9189,13 +9184,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
                             {1, graph.tokens_per_request, graph.value_heads,
                              graph.value_dim}, {}};
   batch_result.dense_strides = MakeDenseStridesForMetadata(batch_result.shape);
-  StaticTensor index_anchor_seed{DataType::INT32, {1, 1, 1, 1}, {}};
-  index_anchor_seed.dense_strides =
-      MakeDenseStridesForMetadata(index_anchor_seed.shape);
-  StaticTensor index_anchor{
-      DataType::INT32,
-      {1, graph.tokens_per_request, graph.value_heads, graph.value_dim}, {}};
-  index_anchor.dense_strides = MakeDenseStridesForMetadata(index_anchor.shape);
 
   const std::string mixed_type = TensorTypeText(graph.mixed_qkv);
   const std::string gate_type = TensorTypeText(graph.a);
@@ -9206,9 +9194,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
   const std::string indices_column_type = TensorTypeText(indices_column);
   const std::string indices_full_type = TensorTypeText(indices_full);
   const std::string state_row_type = TensorTypeText(state_row);
-  const std::string state_anchor_row_type = TensorTypeText(state_anchor_row);
-  const std::string state_anchor_row_bf16_type =
-      TensorTypeText(state_anchor_row_bf16);
   const std::string state_group_type = TensorTypeText(state_group);
   const std::string state_transposed_type = TensorTypeText(state_transposed);
   const std::string qk_box_type = TensorTypeText(qk_box);
@@ -9226,8 +9211,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
   const std::string output_group_bf16_type = TensorTypeText(output_group_bf16);
   const std::string output_box_type = TensorTypeText(output_box);
   const std::string batch_result_type = TensorTypeText(batch_result);
-  const std::string index_anchor_seed_type = TensorTypeText(index_anchor_seed);
-  const std::string index_anchor_type = TensorTypeText(index_anchor);
   const std::string result_type = TensorTypeText(graph.result);
 
   std::string source;
@@ -9284,12 +9267,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
   AppendFloat(source, graph.scale);
   source += " : f32\n    %scale_splat = splat %scale : ";
   source += wide_query_group_type;
-  source += "\n    %one_index = constant 1 : si32";
-  source += "\n    %one_index_splat = splat %one_index : ";
-  source += index_anchor_type;
-  source += "\n    %zero_index = constant 0 : si32";
-  source += "\n    %zero_index_splat = splat %zero_index : ";
-  source += index_anchor_type;
 
   std::vector<std::string> batch_outputs;
   batch_outputs.reserve(static_cast<size_t>(graph.batch_size));
@@ -9310,23 +9287,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
     source += indices_column_type;
     source += " -> ";
     source += indices_full_type;
-    source += "\n    %index_anchor_seed_" + batch_suffix + " = reshape " +
-              index_name + " : ";
-    source += indices_column_type;
-    source += " -> ";
-    source += index_anchor_seed_type;
-    source += "\n    %index_anchor_" + batch_suffix + " = broadcast " +
-              "%index_anchor_seed_" + batch_suffix + " : ";
-    source += index_anchor_seed_type;
-    source += " -> ";
-    source += index_anchor_type;
-    source += "\n    %index_plus_one_" + batch_suffix + " = add " +
-              "%index_anchor_" + batch_suffix + ", %one_index_splat : ";
-    source += index_anchor_type;
-    source += "\n    %valid_state_index_" + batch_suffix +
-              " = cmp %zero_index_splat lt %index_plus_one_" + batch_suffix +
-              " : ";
-    source += index_anchor_type;
     source += "\n    " + state_row_name + " = gather_rows %arg5, " +
               indices_full_name + " : (";
     source += state_type;
@@ -9562,6 +9522,9 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
                 ", %outer_" + suffix + " : ";
       source += state_group_type;
       current_state = updated;
+      if (token + 1 == graph.tokens_per_request) {
+        continue;
+      }
 
       source += "\n    %state_t_" + suffix + " = transpose " + current_state +
                 " permutation = [0, 1, 3, 2] : ";
@@ -9589,26 +9552,6 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
       token_outputs.push_back(output_box_name);
     }
 
-    std::string batch_output;
-    if (graph.tokens_per_request == 1) {
-      batch_output = token_outputs[0];
-    } else {
-      batch_output = "%batch_output_" + batch_suffix;
-      source += "\n    " + batch_output + " = concatenate ";
-      for (size_t token = 0; token < token_outputs.size(); ++token) {
-        if (token != 0) source += ", ";
-        source += token_outputs[token];
-      }
-      source += " dimension = 1 : (";
-      for (size_t token = 0; token < token_outputs.size(); ++token) {
-        if (token != 0) source += ", ";
-        source += output_box_type;
-      }
-      source += ") -> ";
-      source += batch_result_type;
-    }
-    batch_outputs.push_back(batch_output);
-
     const std::string final_state = "%state_final_" + batch_suffix;
     source += "\n    " + final_state + " = reshape " + current_state + " : ";
     source += state_group_type;
@@ -9624,39 +9567,59 @@ std::string EmitNativeTileGdnRecurrentBatchedSource(
     source += state_row_type;
     source += ") -> ";
     source += state_row_type;
-    const std::string anchor_f32 = "%state_anchor_f32_" + batch_suffix;
-    const std::string anchor_bf16 = "%state_anchor_bf16_" + batch_suffix;
-    const std::string anchor_box = "%state_anchor_box_" + batch_suffix;
-    std::string anchor_full;
-    const std::string anchored_output = "%anchored_output_" + batch_suffix;
-    source += "\n    " + anchor_f32 + " = slice " + stored_state +
-              " starts = [0, 0] limits = [1, " +
-              std::to_string(value_width) + "] strides = [1, 1] : ";
+    const std::string stored_group = "%stored_group_" + batch_suffix;
+    source += "\n    " + stored_group + " = reshape " + stored_state + " : ";
     source += state_row_type;
     source += " -> ";
-    source += state_anchor_row_type;
-    source += "\n    " + anchor_bf16 + " = convert " + anchor_f32 + " : ";
-    source += state_anchor_row_type;
+    source += state_group_type;
+    const std::string final_suffix =
+        batch_suffix + "_t" +
+        std::to_string(graph.tokens_per_request - 1);
+    source += "\n    %state_t_" + final_suffix + " = transpose " +
+              stored_group + " permutation = [0, 1, 3, 2] : ";
+    source += state_group_type;
+    source += " -> ";
+    source += state_transposed_type;
+    source += "\n    %output_wide_" + final_suffix +
+              " = matmul(%q_scaled_" + final_suffix + ", %state_t_" +
+              final_suffix + ") : (";
+    source += wide_query_group_type;
+    source += ", ";
+    source += state_transposed_type;
+    source += ") -> ";
+    source += output_group_type;
+    source += "\n    %output_" + final_suffix + " = convert %output_wide_" +
+              final_suffix + " : ";
+    source += output_group_type;
     source += " -> ";
-    source += state_anchor_row_bf16_type;
-    source += "\n    " + anchor_box + " = reshape " + anchor_bf16 + " : ";
-    source += state_anchor_row_bf16_type;
+    source += output_group_bf16_type;
+    const std::string final_output_box = "%output_box_" + final_suffix;
+    source += "\n    " + final_output_box + " = reshape %output_" +
+              final_suffix + " : ";
+    source += output_group_bf16_type;
     source += " -> ";
     source += output_box_type;
+    token_outputs.push_back(final_output_box);
+
+    std::string batch_output;
     if (graph.tokens_per_request == 1) {
-      anchor_full = anchor_box;
+      batch_output = token_outputs[0];
     } else {
-      anchor_full = "%state_anchor_full_" + batch_suffix;
-      source += "\n    " + anchor_full + " = broadcast " + anchor_box + " : ";
-      source += output_box_type;
-      source += " -> ";
+      batch_output = "%batch_output_" + batch_suffix;
+      source += "\n    " + batch_output + " = concatenate ";
+      for (size_t token = 0; token < token_outputs.size(); ++token) {
+        if (token != 0) source += ", ";
+        source += token_outputs[token];
+      }
+      source += " dimension = 1 : (";
+      for (size_t token = 0; token < token_outputs.size(); ++token) {
+        if (token != 0) source += ", ";
+        source += output_box_type;
+      }
+      source += ") -> ";
       source += batch_result_type;
     }
-    source += "\n    " + anchored_output +
-              " = binary_select %valid_state_index_" + batch_suffix + ", " +
-              batch_output + ", " + anchor_full + " : ";
-    source += batch_result_type;
-    batch_outputs.back() = anchored_output;
+    batch_outputs.push_back(batch_output);
   }
 
   std::string result_value;
