"""Reproducible Qwen3.5 release measurements.

The package deliberately keeps orchestration, numerical acceptance, latency
measurement, and profile aggregation in separate modules.  Public entry
points live in ``tools/``.
"""
