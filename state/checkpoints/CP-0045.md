# CP-0045: Bounded fused-pointwise TensorIR frontend v2

- PyPTO `b83fcd3ddc497d585bcc45883eede179aff7d4d2`, tree
  `49eda98f3ed8d72bfd14d5a5900cdc0e71ca699d`, extends the accepted strict
  structured frontend with private `FusedPointwiseV2` while preserving
  `LegacyStaticAddV1` exactly.
- V2 accepts one Opaque function with 1 through 16 static dense BF16/FP32
  inputs, 1 through 64 linear SSA assignments, and one direct final return.
  The registered allowlist is add/adds/sub/subs/mul/muls/neg/exp/recip/rsqrt.
  Scalars are same-dtype finite `ConstFloat` values emitted through canonical
  constant/splat operations. Attributes, kwargs, mutation, older DAG edges,
  dynamic shapes, views and fallback are rejected.
- Schema-2 static/symbolic/argument/result/mutation projections are domain
  separated from legacy V1. Preparation requires one power-of-two flattened
  tile and finalization requires exact static grid
  `{ceil(product(shape)/tile),1,1}`, N input pointers plus Result0, pointer-only
  packing and zero workspace.
- Fresh backend-OFF and backend-ON products are retained under `builds/`.
  OFF native 3/3 and exact-product Python 1/1 pass; ON native 3/3 and
  exact-product Python 1/1 pass. DSO hashes are `eb4225cc...7ab80` and
  `0e8f33c2...facbe`; all four JUnit hashes and every run sidecar are bound by
  EV-0058.
- ON native tests compile FP32/BF16 all-op chains and the maximum
  16-input/64-assignment graph to SM120 Cubins. They also freeze the CP44 FP32
  `[8,8]` and BF16 `[128]` legacy source, all five projections, callable ABI,
  13,784-byte Cubins and Cubin hashes exactly.
- The initial OFF lane demonstrates verified owned pause/timeout recovery. An
  in-source ON build correctly fails the clean-source provenance guard; the
  accepted ON product uses a workspace-local external binary directory and the
  exact clean source worktree. No exclusion or guard weakening was used.
- Independent final evidence review returns GO with P0/P1/P2 = 0.
- Pre-commit root run `pypto-20260826T023019Z-1343454-0edc9e` is diagnostic:
  245 tests plus 126 subtests pass and the sole failure is the GPU-control
  manifest correctly rejecting this uncommitted persistence draft as a dirty
  root. It is not health evidence; rerun the complete root suite from the clean
  checkpoint commit before final handoff.
- CP-0045 accepts source, identity, ABI and CPU TensorIR/CUDA Tile Cubin
  production only. It does not accept V2 GPU launch, numerical/transcendental
  correctness, CUDA Graph/current-stream runtime behavior, performance, full
  PyPTO regression, frameworks, Qwen, or strict coverage.

## Resume action

Create a separately versioned fused-pointwise real-SM120 correctness control
and run representative FP32/BF16 chains through fresh `NvidiaExecutable`
lifetimes on a non-default current stream. Preserve CP44 controls and legacy
anchors byte-for-byte. In parallel, the reviewed RowReductionV3 source head
`17b2b3c` may enter its own OFF/ON compiler gate; do not treat its source review
as Cubin or runtime evidence. StructuredMatmulV4 follows reduction.
