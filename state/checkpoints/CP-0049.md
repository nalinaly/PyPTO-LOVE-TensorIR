# CP-0049: RowReductionV3 real-SM120 ten-case correctness gate

Status: accepted (EV-0062).

## What is accepted

Run `pypto-20260826T175445Z-218543-3ae1ad` executed the fixed ten-case
RowReductionV3 matrix (FP32/BF16, rank 1-3, sum/max, twenty fresh
executable lifetimes) on the real RTX 5090 Laptop GPU through the frozen
policy-2 protected-zero-NVIDIA controller. The CPU-only finalizer joined
every record to the frozen oracles, compile anchors, sidecars and replay
bins and published the immutable report
`reports/data/pypto-row-reduction-sm120-pypto-20260826T175445Z-218543-3ae1ad.json`
(306,721 bytes, SHA-256 `564ae535b9e339cc1d0ca7198e066450f64a1c060c07a67e017a1a5631fd7e35`).
An identical finalizer retry failed no-replace with the report unchanged.
Independent review is GO with P0/P1 zero.

## What is NOT accepted

General reduction correctness, unlisted shapes/dtypes/domains,
performance, CUDA Graph, framework integration or model claims. The
CP47/CP48 reports and bytes are not reinterpreted.

## Path to acceptance (three-run history)

1. `pypto-20260826T161547Z-146383-954cc0` (v1 controls) failed at
   `rank1_fp32_sum_n1` repetition 1: torch-sign-anchored special
   comparison rejected the row, and no output bytes were dumped.
2. Control revision 2 (`da3ca04`/`b43caf8`) anchored special class/sign
   to the frozen CPU contract, added provider zero-sign tolerance,
   three-way exact equality and pre-comparison word dumps. Run
   `pypto-20260826T165111Z-173109-3f7a6a` then failed the SAME row with
   decisive evidence: kernel actual `0x80000000` (-0) vs contract and
   torch `0x00000000` (+0) — a genuine kernel defect, not a control
   defect.
3. PyPTO `b5e5cae`+`faefd0a` added the explicit `+0` accumulator
   epilogue to every FP32-domain row_sum reduction (row_max untouched).
   Fresh backend-ON build passes native 13/13 and the exact-product
   Python suite; compile anchors regenerated from two clean CUDA-hidden
   runs with the CP48 max overlaps still byte-exact and the sum overlaps
   recorded as deliberate divergence. Clean post-manifest full root
   suite: 395 tests plus 358 subtests. The accepted run then passed all
   twenty lifetimes with the historical row producing `+0`.

## Control identities

- Root implementation `6a4101c`, manifest-only `39b4c35`, control
  manifest SHA-256 `6d9a9fbf507c6f3a2fe757e37c4574055136266f825c15c18d5396195b2b5b13`.
- PyPTO `faefd0a1df29b4bdb3434f5f6e5f73757387ae3e` (tree `8da7235e...`),
  ON DSO 784,342,176 bytes, SHA-256 `c72fdf3c...ea0e5`.
- Compile anchors 112,915 bytes, SHA-256 `994da1c7...cd47`, anchor runs
  `pypto-20260826T174245Z-209241-eb855d` and
  `pypto-20260826T174333Z-209547-311e35`.
- Off-product build `builds/pypto-row-reduction-sumfix-off-b5e5cae`
  passes native 11/11 with the updated goldens.
