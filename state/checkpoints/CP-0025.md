# CP-0025: Accepted pointer-free KernelBuildSpec v1

- PyPTO `9b3cf71b6ff2a535aabdd053684a60b47450fac0` adds the
  immutable, value-owned, pointer-free per-region `KernelBuildSpec` v1 data
  contract, separately from producer composition, artifacts, cache and runtime.
- The canonical identity binds source/IR and callable ABI digests, one generic
  semantic route, a full pipeline revision, all eight explicit resolved
  schedule categories, static/symbolic specialization, argument/result/mutation
  ABI, the parent CompileRequest byte identity and optional all-or-none operator
  catalog provenance.
- Typed schedule scalars, strict name order, one-MiB payload cap and explicit
  MessagePack array/map/string/bin/ext/depth limits prevent ambient defaults,
  ambiguous encodings and allocation-bomb decoding.
- Native CTest passes 4/4. The final exact-source bootstrap loads DSO
  `8b999de1...c9806`, executes three native compiler contracts, and passes the
  Python compiler suite 122/122 in run
  `pypto-20260824T112218Z-196243-e07a11`. Its dependency closure is only the
  five standard runtime libraries; CUDA, Triton and TensorIR are absent.
- The post-persistence root control regression passes 197 tests plus 98
  subtests in run `pypto-20260824T112633Z-197902-eb96e2`, with no coexistence
  pause or abort.
- Independent review caught that the ordinary editable finder selected an
  older physically installed DSO after the final digest refactor. That green
  test was demoted; the exact current source DSO was explicitly bootstrapped
  and rebound in EV-0038. One earlier bootstrap quoting failure is also retained.
- This checkpoint does not compile a kernel, bind an exact TensorIR/CUDA Tile
  producer, create Artifact/ArtifactCache/NvidiaExecutable, initialize CUDA,
  register a framework backend, or claim operator/model correctness, coverage
  or performance.

## Resume action

Implement the next isolated compiler data boundary: exact LLVM/tileiras
producer identity plus bytes-and-metadata `Artifact` validation. Keep loaded
CUDA modules, CUcontexts, streams and launch state out of that persistent
artifact. In parallel, finish the already-running RPATH-free pinned Triton
wheel audit/probe transaction before any environment replacement or GPU smoke.
