# CP-0004: Native build closure and typed operator semantics

- The PyPTO compiler sources now build successfully through an internal object
  target embedded in the existing single extension DSO. The corrected PUBLIC
  runtime-ABI include requirement is present in the binding compile command.
- The object-boundary focused suite passes 486 tests. The full suite reports
  10,173 pass, 58 skip, and three reproducible parser/test-harness failures;
  therefore the object-target transaction and fresh wheel gate remain pending.
- A false-positive traceback test was corrected in its own PyPTO commit so
  Conda CPython's legitimate `/usr/local/src/conda/...` frames remain visible.
- `pypto-kernels` now owns immutable, explicit-ABI typed semantic families for
  matmul, paged attention, and GDN. This is semantic ownership only; no CUDA
  kernel, correctness, or performance claim exists yet.
- PyTorch's exact Triton pin is materialized as a clean official standalone
  checkout under `upstream/triton`; the inherited external editable install is
  still rejected until a project-local wheel replaces it.
- A fresh PyPTO wheel build was not started after zcode launched its own TP=2
  SGLang/gem5 run. No protected process was signalled or otherwise touched.

