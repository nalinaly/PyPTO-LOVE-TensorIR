# CP-0026: Frozen exact Triton reference wheel

- The exact PyTorch-pinned Triton source `5d6048aa...` now has one workspace-
  local, networkless-built CPython 3.14 wheel with SHA-256 `1d58d830...6227a`.
- Complete source/dependency/producer/RECORD/native audit passes in run
  `pypto-20260824T134836Z-217013-12ae6e`; the pip-free fresh-prefix probe passes
  in `pypto-20260824T134906Z-217357-e68fd4`.
- All 18 native ELFs have no RPATH/RUNPATH. Seven stripped NVIDIA vendor ELFs
  lack GNU Build IDs and are accepted only by an exact closed path/SHA set.
  Three upstream example plugins resolve `libtriton.so` only through the
  wheel-internal `triton/_C` audit path.
- The initial build run returned nonzero only after producing the unchanged
  wheel because the older auditor required a Build ID from vendor `cuobjdump`.
  That recovery lineage, the earlier RPATH-rejected candidate, and all build
  evidence remain retained.
- This is a reference/baseline dependency only. It is not installed yet and is
  forbidden as model-forward compute in final strict PyPTO runs.

## Resume action

Freeze Triton here. Move the active implementation lane to PyPTO's private
TensorIR/CUDA Tile/exact-LLVM static build composition and deterministic
artifact seam. Defer the reversible Triton environment replacement and
reference-only GPU smoke until the unmodified SGLang baseline gate needs them.
