# CP-0012: Source-reviewed SM120 TargetInfo candidate

- A separate PyPTO worktree at `projects/pypto-target-info` is clean on
  `feature/nvidia-target-info` commit
  `9939b885041b931284f7ce56ecbf888601b60b6e`.
- It appends `BackendType::Nvidia` without renumbering Ascend, while refusing to
  fabricate a legacy Backend/Handler/SoC. Every known legacy entry now rejects
  target-only identity before global state, platform, path or sidecar access.
- Immutable TargetTraits carry explicit SM120 launch resources including
  default versus opt-in CTA shared memory, thread/block/grid and register limits.
  NvidiaTargetInfo derives `sm_120/sm_120a` and binds CUDA, TensorIR/CUDA Tile
  revisions and the exact BF16/FP32 compute allowlist.
- The TensorIR hard-coded cc>=100 shared-memory assumption is explicitly a
  future patch/filter gate. Current data is not launch-legality proof.
- C++ source, nanobind, Python package/stubs, legacy consumers, C++/Python tests,
  English/Chinese docs and MkDocs nav are present. Python compileall, diff check
  and docs path parity pass; three independent reviews report no P0/P1.
- This candidate is deliberately unbuilt and unverified. No CMake/native test,
  binding import, wheel, TensorIR bridge, CUDA query, compile or launch ran.
- The candidate must not be merged before the staged single-DSO object-boundary
  commit is fully validated and committed; their CMake overlap is intentional.
- Protected zcode work remained active and no external process was signalled.

## Resume action

Run the heavy preflight. When green, finish the current PyPTO full-suite/fresh
wheel gate and commit only its two CMake files. Then apply candidate `9939b88`,
resolve CMake overlap, materialize its worktree submodules, and run all native,
Python, wheel and Ascend-regression gates before calling TargetInfo implemented.
