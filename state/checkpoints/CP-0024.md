# CP-0024: Accepted reviewed exact Triton dependency closure

- Root commits `ea39ac5`, `f678fc0`, `5c75ea4`, `fe903fa`, and `cca595c`
  close bounded download, strong-ETag Range resume, durable no-replace
  publication, verified-stop exit races, deterministic extracted modes and the
  source-reviewed ten-archive lock.
- Materialization run `pypto-20260824T093330Z-135732-e907f1` atomically
  published canonical manifest
  `29c0736211ba0b286acd562ba097d7f1dea989671003c63a7b988de5afb0fe7d`.
  LLVM came from a separately hashed workspace seed; nine network archives each
  used one strong-ETag HTTPS response and no resume. No downloaded executable
  ran before review.
- Two independent reviews rehashed all archives and expanded trees, compared
  5,853 expanded files / 5,693,007,907 bytes, audited 6,503 archive members and
  31 safe symlinks, and proved the 167-leaf NVIDIA overlay is exact. Seven
  NVIDIA hashes also match official CUDA 12.8.1/13.1.0 redistribution manifests.
- Source-lock commit `cca595c8f064058f0d72f160122f68a1d6577934`
  freezes all ten SHA/byte pairs and the externally supplied manifest anchor.
  Cross-umask 0022/0077 materialization produces identical canonical tree
  identities. Independent re-review reports no P0/P1/P2.
- Promotion run `pypto-20260824T100412Z-150285-bbfebd` added the exact
  `review.json`, passed require-reviewed verification, executed only
  networkless/read-only sandboxed tool-version probes, and moved the tree with
  cross-parent `RENAME_NOREPLACE` plus both-parent fsync into
  `caches/triton-build-deps/29c073...`. Final require-reviewed verification
  passed.
- Earlier truncated/slow/misrouted attempts are retained. All three intentional
  stops targeted only verified workspace PGIDs. The final stop-race hardening is
  committed as `fe903fa`; no protected zcode/amdgpu process was signalled.
- The dependency closure is accepted for workspace-local build use. Publishing
  or redistributing a later wheel remains blocked on a separate wheel-level
  license review.
- This checkpoint does not build/audit/install Triton, run the SM120 reference
  smoke, implement PyPTO CUDA, or claim model coverage/performance.

## Resume action

Execute section 3 of `docs/triton_workspace_wheel_acceptance_gate.md` using the
exact reviewed cache path and source commit. The offline build remains a
non-benchmark CPU-only coexistence action. Stop after wheel audit and fresh
probe; the reference-only SM120 smoke requires a separate exclusive GPU gate.
In parallel, the next PyPTO compiler transaction remains the pointer-free
per-region KernelBuildSpec.
