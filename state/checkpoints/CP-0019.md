# CP-0019: Acceptance-gate repair and NVIDIA artifact boundary

- Heavy preflight still returns 75 for protected `zcode-vllm-tp2-v4`; no
  native build, full suite, wheel, CUDA launch or model process started.
- The two staged single-DSO CMake files remain unchanged and uncommitted. A
  final read-only review found no source P0/P1.
- `docs/single_dso_acceptance_gate.md` now performs a real venv wheel install,
  verifies the console script, enumerates every wheel DSO/duplicate member,
  fails on `ldd` errors/forbidden dependencies, audits every native/binding
  compile row and enforces the exact two-file commit boundary. All five Bash
  blocks pass syntax validation; final review reports no P0/P1.
- A read-only 34-path application check proves TargetInfo candidate `9939b88`
  should apply after the DSO commit without a text conflict. The overlapping
  CMake files retain object-target ownership while adding `target_info.cpp` and
  `modules/compiler.cpp`.
- `docs/target_info_acceptance_gate.md` freezes ordered cherry-pick, fresh
  native 2/2 CTest, dual target-info compile-row ownership, fresh one-DSO wheel,
  installed-DSO dependency audit, real venv import, 31 targeted tests and the
  expected 10,208-pass/58-skip full gate. All four Bash blocks pass syntax
  validation; final review reports no P0/P1.
- TensorIR source review proves `ICompiler::compile()` returns a process-local
  `IRuntimeKernel`, while complete argument/grid reconstruction metadata lives
  in `CudaTileFrontendResult` and private strategy objects. Runtime support and
  workspace checks are TODO; initialization is not actually synchronized.
- Decision D-0009 and `docs/compile_request_artifact_design.md` split the next
  work into data-only CompileRequest, per-region KernelBuildSpec, exact
  LLVM/tileiras producer identity, deterministic bytes-plus-metadata Artifact,
  persistent cache, and process/device/CUcontext-bound prewarmed executable.
- The design also freezes parent-only target query, 82-SM/101376-byte SM120
  resource correction, strict rejection of ambient TensorIR byte overrides,
  requested-versus-actual artifact checks, non-null/context-compatible stream
  law, worker/PassContext isolation and correct CUDA Graph capture/replay
  ownership. Final review reports no P0/P1.
- This checkpoint repairs source/design gates only. It claims no accepted DSO,
  built TargetInfo, CompileRequest implementation, TensorIR composition,
  artifact, runtime launch, model correctness, coverage or performance.

## Resume action

Wait for the protected lane to exit naturally and rerun
`python tools/preflight.py --mode heavy`. If green, execute
`docs/single_dso_acceptance_gate.md`, commit only its two CMake files, and
persist that runtime/build evidence. Then apply and execute
`docs/target_info_acceptance_gate.md`. Only after TargetInfo acceptance may the
data-only CompileRequest and KernelBuildSpec transactions begin.
