# CP-0036: CUDA parameter-ABI enumeration repair and SM120 smoke manifest v3

- Retained run `pypto-20260825T052038Z-800777-8e8e83` is preserved as a
  fail-closed diagnostic, not accepted GPU behavior. Root `4f2d500` and PyPTO
  `6361f11` passed parent admission, pre-release and child isolation, observed
  the real RTX 5090, PyTorch Runtime provider and regular CUDA context, compiled
  all three Cubins, and loaded the first static module/function. Static
  repetition zero then failed inside `NvidiaExecutable::Prewarm` before
  `prepare_launch`, launch, provisional publication or numerical comparison.
- The failed run exited `rc=1`; its pre-release, last and post-exit external
  compute-PID sets and protected compute-PID/runtime-mapping/unreadable-map sets
  are empty. PGID `800865` has no survivor. No external process was signalled.
  The retained static Cubin SHA `6dc121d2...32b82` equals the replay Artifact
  bytes. Cuobjdump is a separate post-run diagnostic, not child stderr evidence.
- PyPTO `206447cf8c68b9cff1b86e01f0b40bfd689cd7a7`, tree
  `e0357daaefa74dbf676550015e60701996c400fb`, repairs the generic parameter ABI
  without changing the Artifact schema, pipeline revision or expected Cubin
  bytes. Cubin `KPARAM` ordinals remain the kernel-signature authority. Live
  Driver enumeration is checked as a width multiset plus bounded offset ranges,
  independent of enumeration order; the launch pointer array remains in
  signature order.
- Dynamic tensor size/stride parameters match the producer's existing
  four-byte `int32` ABI. Positive sizes and nonnegative strides above
  `INT32_MAX` are rejected before packet publication; exact native `int32`
  bytes are copied into zero-initialized stable eight-byte host storage. Tests
  cover an exact non-palindromic reversed mixed-width enumeration, old
  eight-byte dynamic widths, overlap, out-of-frame and overflow failures, value
  limits, and signature-ordered launch pointer values.
- CUDA Tile README's launch contract is authoritative: Tile Grid is supplied by
  grid dimensions, while host block dimensions are unused and must be
  `[1,1,1]`. Cubin `EIATTR_REQNTID=[128,1,1]` is consistent with four selected
  worker warps and must not be copied into `CUlaunchConfig.blockDim*`. The exact
  metadata-to-worker mapping remains a compiler inference. The experimental
  opposite change was reverted; private TensorIR is clean at `1dcb38c`.
- Fresh backend-ON build `pypto-20260825T065050Z-869893-badb83` produces the
  780,535,416-byte DSO with SHA-256
  `15675c471f507b97190b0a770bb16e821c5e99353b65bbbc019988490f59018c`.
  CTest run `pypto-20260825T065932Z-883420-3d116c` passes 9/9; exact-DSO run
  `pypto-20260825T070443Z-889280-424570` passes Python 142/2.
- Fresh backend-OFF build `pypto-20260825T070007Z-883835-7cb6d3` produces the
  434,646,736-byte DSO with SHA-256
  `32c2dea03e13ea49937df239ed1e7bc2b8a931594c41dcfc0cc6de8375464109`.
  CTest run `pypto-20260825T070412Z-889089-b986bd` passes 7/7; exact-DSO run
  `pypto-20260825T070508Z-889497-469338` passes Python 135/9.
- Audit run `pypto-20260825T070539Z-889610-55d705` proves both DSOs have no
  RPATH/RUNPATH, exactly five standard dependencies and two permitted dynamic
  definitions, no CUDA/TensorIR/NvidiaExecutable symbol leakage, and the exact
  mutually exclusive ON/OFF production and test compile rows.
- CPU-only run `pypto-20260825T070649Z-890066-2a1ae2` preserves the isolated
  runner's ownership markers and empty `CUDA_VISIBLE_DEVICES`, observes Torch
  device count zero with CUDA uninitialized, and deterministically recompiles
  all three expected Cubin size/SHA and ABI pairs through the exact final DSO.
  Triton, SGLang and FlashInfer are absent. Earlier `062710`, `062751` and
  `070611` runs remain diagnostics: the first two reset wrapper-owned state in
  an inner environment, and the last asserted an obsolete ownership marker.
- Intermediate PyPTO `3431a94` plus root control v2 `8176731`/`1cb5638` are
  retained immutable predecessor evidence, but they are not authorized for the
  next GPU smoke because v2 still joined live widths by Driver enumeration
  index.
- Root Layer A3 `c71f32bd415a973a2a7756ecc9b1ae59f30df219`, tree
  `89dc321ff8f6913b826f70f09c07d5c045a67790`, binds PyPTO `206447c` and the
  final backend-ON DSO while preserving the fixed runner. Manifest-only Layer
  B3 `3de4cf702662cbaf948c6429acf269fee16a491e`, tree
  `c72970aa075e5769aad945599947b1764440f2c6`, adds
  `state/contracts/pypto_nvidia_executable_sm120_v3.json`, 1,569 bytes, SHA-256
  `978e873788eb7f3aaeba6473a9b7f8a1bcd827fe201d89cb781927f538c9b6e3`.
  Manifests v1 and v2 remain unchanged.
- Clean post-v3 root run `pypto-20260825T071601Z-892819-67acee` passes 224
  tests plus 106 subtests. JUnit SHA-256 is
  `cbc276bd9a9869f8afb6345e3c3f1ebfa8c3501070a2401ecf5a101bdab7b213`.
  Six protected heavy processes were present, protected NVIDIA compute PID set
  was empty, no coexistence pause occurred, and no external signal evidence
  exists.
- CP-0036 records and preserves the failed-run boundary, and accepts only the
  generic parameter ABI/enumeration repair, fresh supporting CPU validation,
  CUDA Tile logical-host launch semantics, and immutable v3 control identity.
  It does not accept a
  successful PyPTO CUDA kernel launch, CUDA numerical correctness, CUDA Graph,
  frontend lowering, operator kernel, TorchInductor/SGLang registration, Qwen
  correctness, strict coverage, profiling or performance.

## Resume action

Run the exact v3 `gpu-smoke` command from
`docs/pypto_nvidia_executable_sm120_smoke.md` only after a fresh green
admission. If and only if it publishes a provisional result and exits cleanly,
run the no-site CPU finalizer, retain its final report and obtain independent
evidence/scope/persistence review before advancing frontend lowering. Never
change CUDA Tile host block dimensions from `[1,1,1]` to match internal
`REQNTID`. If the next prewarm stops at occupancy/resource validation,
investigate and establish the missing tile-aware physical-residency evidence
rather than changing the logical launch block. Never signal protected
amdgpu-sim/zcode processes.
