# CP-0014: Producer-owned operator ABI and framework guard

- `pypto-kernels` is clean at
  `ccb4999f4563410f569a5a823c45ddb594183687` and publishes the canonical
  framework-adapter ABI schema/digest from its real runtime contracts.
- `pypto-framework-plugins` is clean at
  `92721a29ea095c8b239a77118b328a75534309a5`; it no longer carries a second,
  partial matmul/attention/GDN ABI schema.
- The plugin independently recomputes the full ABI digest, re-derives every
  declared runtime binding, pins the source-tree digest, and binds imports to
  either exact wheel RECORD ownership or strict PEP-660 `direct_url.json`.
- Current pure-Python packaging rejects native extensions and sourceless
  bytecode until executable payload identity/readiness is designed explicitly.
- Torch and SGLang entry paths run operator identity and executable readiness
  before framework imports or registry/config mutation. Pre-strict failures
  cross ordinary fallback boundaries with `SystemExit`.
- Kernel validation is 102 tests plus 65 subtests. Plugin validation is 118
  tests. Compileall, clean isolated wheel install, and a real PEP-660 editable
  install pass. Three independent reviews report no P0/P1.
- This checkpoint proves project separation and package/ABI identity only. It
  does not claim executable PyPTO kernels, framework registration, CUDA/model
  correctness, coverage or performance.
- The live heavy-work preflight is green at this observation. Protected zcode
  processes still exist and remain untouchable; preflight must be rerun before
  each heavy action.

## Resume action

Rerun the staged PyPTO single-DSO full suite and build/test a fresh wheel. Only
if both gates pass, commit exactly its two CMake files. Then apply the separate
SM120 TargetInfo candidate, resolve the expected CMake conflict, and perform
its own native/Python/wheel/unchanged-Ascend validation.
