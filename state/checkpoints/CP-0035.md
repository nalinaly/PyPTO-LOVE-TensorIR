# CP-0035: Accepted SM120 correctness-smoke control path

- Root implementation `394b75adbc7babe1000d93938e6fa84493a4277d`
  (tree `8b80ab64ee6c0fa15c27e7249da1fb4f9995c725`) adds the
  fixed PyPTO NvidiaExecutable correctness runner, zero-NVIDIA protected-CPU
  admission/watchdog, replay artifacts and CPU-only finalizer.
- Manifest-only root commit `2b53f0a6cdeffa89b38ad75515b9ea1d1019748a`
  (tree `ccda1b64f1df64b1428c409c910081333b36e20d`) adds
  `state/contracts/pypto_nvidia_executable_sm120_v1.json`, 1,566 bytes with
  SHA-256 `c609e97a2f3e379e332137916d041d14931c0e415cd2f2e769c82eab1650aa09`.
  It binds seven ordered control blobs to the implementation commit/tree and
  requires a clean descendant root with no later control-path change.
- Controller, finalizer and both GPU-smoke preflights require `-E -B -S` and a
  sanitized environment. The exact child uses `-I -B -S`, empty `PYTHONPATH`,
  no SGLang plugins and `PYPTO_ALLOW_FALLBACK=0`. A malicious-PYTHONPATH test
  proves controller/finalizer imports cannot be shadowed before validation.
- The explicit correctness-only coexistence policy permits the protected CPU
  lane only while every protected `/proc/<pid>/maps` is readable and contains
  no NVIDIA runtime mapping, the protected/foreign NVIDIA compute-PID sets are
  empty, host/GPU/disk floors pass and the exact selected Python/Torch/
  libcudart/PyPTO/source identities match. Performance `gpu-benchmark` remains
  exclusive.
- After `Popen`, the direct child waits. The parent durably records ownership,
  revalidates exact inputs plus live NVIDIA isolation, publishes a gate, writes
  release metadata, and makes the no-replace barrier the commit point. The
  child repeats the audit before importing Torch. The periodic and post-exit
  audits classify owned compute only by PID start-tick, descendant lineage and
  the recorded PGID. Every signal still passes `stop_run` ownership checks.
- The runner contains three generic TensorIR fixtures: static FP32 add,
  dynamic-stride FP32 add and FP16 tensor plus by-value FP32 scalar. Their ABI,
  grids and deterministic SM120 Cubin sizes/SHA values match the accepted C++
  fixtures and CPU-only compiler. Each Artifact receives two fresh executable
  lifetimes on one selected non-default PyTorch current stream; packet lifetime,
  external synchronization, logical output, read-only input storage, dynamic
  padding, context address/ID and terminal explicit unload are required.
- Replay contains the serialized CompileRequest, three KernelBuildSpecs and
  three Artifacts. The no-site finalizer reopens the exact PyPTO DSO and
  independently reconstructs complete TargetInfo, source/spec/ABI/Artifact/
  Cubin identities before joining process, preflight, gate, barrier and
  provisional evidence. Exact schemas reject unknown model, coverage, graph or
  performance claims.
- Post-manifest root run `pypto-20260825T050233Z-793333-c8f8cb` passes 224 tests
  plus 106 subtests. Its JUnit SHA-256 is
  `61104d579f8a5d619f1d0b0bac94e56ad7671d3bd1ead1b2258859030c1c0348`.
  CPU-only exact-DSO semantic replay/TargetInfo run
  `pypto-20260825T045248Z-788969-a1b6bc` returns zero and leaves the production
  PyPTO package with zero ignored shadow files.
- An earlier diagnostic accidentally materialized Python caches and one stale
  ignored source-package DSO. All generated material was moved intact under
  `runs/quarantine/`; nothing was deleted. Parent, child and replay now reject
  any ignored PyPTO package shadow.
- Therefore CP-0035 accepts only the smoke implementation, source/control
  provenance, CPU compilation/replay semantics and safe execution policy. It
  does not accept real PyTorch libcudart observation, CUDA context/device
  observation, libcuda module load, kernel launch, numerical GPU correctness,
  CUDA Graph, frontend lowering, operators, frameworks, Qwen coverage or
  performance.

## Resume action

Run the exact `gpu-smoke` command from
`docs/pypto_nvidia_executable_sm120_smoke.md` only after a fresh admission is
green. If it succeeds, finalize the printed provisional SHA in a separate
no-site CPU process and independently review the retained evidence before
advancing frontend lowering. Never edit the manifest-bound controls to work
around a failed smoke.
