# CP-0009: Paged-attention numerical reference

- `pypto-kernels` is clean at
  `ae68359d1c8e5a8b0203a8c29d6f9d2aae812e62`.
- The paged-attention ABI now has a deterministic standard-library numerical
  reference for BF16 storage, FP32-rounded accumulation, causal packed
  prefill/decode, GQA mapping, append-before-attention cache mutation and zero
  inactive output capacity.
- A prefill cache feeds decode in the state-continuity test. Active logical KV
  slots are normalized back to BF16 in returned state; inactive slots are not
  read or changed, and caller inputs remain immutable.
- Tensor and metadata inputs are snapshotted before capacity derivation,
  validation and computation, closing stateful-Sequence TOCTOU attacks.
- 71 tests plus 35 subtests, compileall, wheel build and clean import pass.
  Three independent reviews report no P0/P1 after all fixes.
- This has not been compared against PyTorch/FlashInfer and is not CUDA kernel,
  model correctness, graph, integration or performance evidence.
- Heavy work remains paused for the protected zcode TP2 vLLM/gem5 lane; no
  external process was signalled.

## Resume action

Use this reference for the first CUDA Tile attention correctness tests only
after the PyPTO single-DSO, SM120 target and CUDA Tile launch foundations land.
Keep FlashInfer as a baseline, never as a strict candidate fallback.
