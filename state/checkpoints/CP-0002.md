# CP-0002: Isolated environment and models

- `envs/pypto-nvidia` is an independent clone of the approved `triton-dev`
  environment; source and destination Torch files have distinct inodes.
- PyTorch is `2.13.0+cu130` at `cf30153...`, HIP is absent, and the installed
  Torch package plus dist-info hash to the identity in `ENVIRONMENT.lock`.
- Qwen3.5-0.8B and Qwen3.5-9B are ordinary independent copies under `models/`.
  Every imported file is read-only, has one hard link, and passed a destination
  SHA-256 verification against `models/MANIFEST.json`.
- The preflight was checked before each heavy transaction and periodically
  during large model copies. No protected process was signalled.
- No model execution, compiler build, or runtime acceptance is claimed.

