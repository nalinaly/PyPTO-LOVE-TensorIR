# CP-0011: GDN paired-state numerical reference

- `pypto-kernels` is clean at
  `1a46502e509ed970b7589b8580f4e30acec34c11`.
- GDN ABI v1 now has a deterministic standard-library reference implementing
  raw-history causal conv, BF16 storage, FP32 Q/K normalization and gates, the
  FP32 gated-delta transition, output-from-updated-state and paired state return.
- One-shot prefill, checkpoint-boundary segmented prefill and token-at-a-time
  decode produce exact matching outputs plus final conv and recurrent state.
- Multi-request slot reorder, W=1 empty state, GQA grouping, conv weight order,
  inactive NaN isolation, input immutability and one-read snapshot behavior are
  covered.
- Shared BF16/FP32/snapshot helpers were extracted and the existing paged
  attention reference suite remains green.
- 90 tests plus 61 subtests, compileall, wheel build and clean import pass.
  Three independent reviews report no P0/P1.
- This has not been compared against the pinned PyTorch/SGLang implementation
  and is not state-lifecycle, CUDA, graph, integration or performance evidence.
- Heavy work remains paused for the protected zcode TP2 vLLM/gem5 lane; no
  external process was signalled.

## Resume action

Once the candidate framework environment is hermetic, cross-check both CPU
references against independent Torch baselines. CUDA GDN work must still wait
for the PyPTO object-target, SM120 TargetInfo and CUDA Tile launch gates.
