# CP-0048: RowReductionV3 compiler and Cubin production

- Source commits `1daa7e5` and `17b2b3c` add the bounded static dense
  RowReductionV3 family. The first real build exposed deleted assignment in two
  immutable-descriptor negative fixtures; test-only commit
  `62eb88251df5bdad95277a9d619d20da9bf121eb`, tree
  `04d3bca3...e25c67`, reconstructs complete vectors without changing the
  shape/stride rejection semantics. Two independent reviews return GO with
  P0/P1/P2 zero.
- Fresh clean-commit backend-OFF and backend-ON products are retained under
  `builds/pypto-row-reduction-v3-{off,on}-62eb882-final`. DSO SHAs are
  `95fc6579...93451` and `e1213cf3...e4220`; neither has RPATH/RUNPATH and both
  depend only on five system libraries.
- OFF CTest passes 11/11 and exact-product Python passes 1/1. ON CTest passes
  13/13 and exact-product Python passes 1/1. Every child is CUDA-hidden, binds
  the exact source/product origin and records no compute PID or framework
  launch.
- Machine-readable report
  `reports/data/pypto-row-reduction-v3-compiler-cubin-records.json`, SHA
  `d06765be...38abb`, binds four rank-1/2/3 FP32/BF16 sum/max fixtures. Every
  Artifact has entry `pypto_row_reduction_v3`, route `structured-tensorir`, two
  pointers, zero workspace, legal static grid/tile, nonempty self-hashed SM120
  Cubin and `fallback_used=false`.
- Independent CUDA-hidden run `pypto-20260826T090758Z-1508157-9870e7`
  recompiles all four fixtures from the retained ON DSO and matches every
  source, BuildSpec, Artifact, Cubin and ABI field exactly with Torch CUDA
  uninitialized and no runtime launch.
- The isolation watchdog twice paused only this project's verified build group
  during unrelated external CUDA-compiler memory spikes and resumed it after
  recovery. No external process was signalled.
- EV-0061 accepts only host compiler/Cubin production for four fixed fixtures.
  It does not accept GPU load/launch, numerical correctness, general reduction,
  profiling or performance.

## Resume action

Create a separate RowReductionV3 real-SM120 correctness family. Cover FP32 and
BF16 sum/max, rank-1/2/3, exact and tail row tiles, BF16 FP32-accumulate
semantics, output/input canaries, non-default current stream, packet release
and terminal unload. Keep the compiler/Cubin report immutable. After numerical
acceptance, replay StructuredMatmulV4 commits onto `62eb882` (its original
branch is based on `17b2b3c`), resolve only the test-fixture overlap, then run
the same OFF/ON/compiler/runtime ladder.
