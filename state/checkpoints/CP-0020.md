# CP-0020: Protected external workload blocked audit

- The same external blocking condition has repeated for three consecutive goal
  turns, satisfying the formal blocked-audit threshold.
- Latest `python tools/preflight.py --mode heavy` returns 75.
- Seven protected processes from `zcode-vllm-tp2-v4` remain: the TP=2 vLLM
  lane and three gem5 instances are still externally owned and untouchable.
- MemAvailable is 28.5 GiB, below this workspace's 32 GiB heavy-action floor.
- GPU identity remains the isolated NVIDIA RTX 5090 Laptop/SM120 path; no
  PyPTO heavy build, wheel, CUDA launch or model process started.
- No external PID was signalled, killed, paused or cleaned. No restart occurred.
- Control head before this checkpoint is `3ce8a2d...`; PyPTO remains at
  `f550d4c...` with exactly the two intended tracked CMake modifications and
  ignored NVIDIA submodule cache dirt. TargetInfo candidate remains clean at
  `9939b885...`; official PyTorch/SGLang/Triton remain zero-diff.
- All safe ordered preparation is complete at CP-0019: both acceptance
  runbooks and the D-0009 artifact boundary have final no-P0/P1 reviews.
- The full project objective remains active and unmet. This checkpoint is not
  compiler, runtime, operator, model, coverage or performance acceptance.

## Resume condition

After the protected lane exits naturally and memory recovers, resume the goal
and run `python tools/preflight.py --mode heavy`. Proceed only on exit zero,
starting with `docs/single_dso_acceptance_gate.md`; otherwise remain blocked.
