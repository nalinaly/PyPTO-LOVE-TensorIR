# CP-0018: Pinned TorchInductor zero-diff backend surface inventory

- `pypto-framework-plugins` is clean at
  `3f9d712c6bc522552426ee77e0137e0746bf27a6`.
- A 31-source SHA/AST inventory freezes four pinned Inductor lanes:
  registry/cache, scheduler/wrapper/current stream,
  template/lowering/fallback, and Triton/CuTeDSL/autotune references.
- A canonical manifest covers all 346 Python files under `torch/_inductor` at
  digest `3bef71727acfceb4c1dbc1f433ac26e21c65ff715435feefc80cc32d4cb88cd6`.
  The filesystem and pinned Git tree both contain exactly 346 files.
- The audit proves this PyTorch pin has no TileKernel/cuTile abstraction. It
  does have CuTeDSL, a closed native CUDA selector, and an unconditional
  scheduler `has_triton()` capability check.
- The audit freezes eight `get_current_backend()` call sites, complete
  `DeviceCodegen` fields and key callable signatures/defaults/decorators,
  codecache registry-key consumption, and CUDA raw/current-stream sources.
- Strict blockers are source-derived rather than implied: CSEProxy only
  propagates dtype/shape for Triton/CPP/MPS, extern nodes bypass backend
  scheduling, foreach hard-codes three upstream scheduling classes,
  multi-template can select extern, and MM/BMM/MM-plus-MM declare 12 explicit
  `ExternKernelChoice` producers.
- Immutable scope keeps registration, atomic registry install, Python and
  subgraph wrappers, PyPTO CSE propagation, strict choice filtering and
  subprocess compile unimplemented. No Torch source is modified.
- Plugin tests pass 128. The wheel SHA-256 is
  `8b05bc88cf6a32d07e9faf07e2af1eb361fa2de90e3b50c7f93bfdf6ef91b9ad`;
  isolated install `builds/plugin-inductor-inventory-probe.LcfnCR` verifies the
  canonical operator ABI, 31-source audit and no Torch/Triton/SGLang/PyPTO
  compiler import. Three final reviews report no P0/P1.
- This checkpoint is reconnaissance only. It claims no backend registration,
  code generation, CUDA launch, model correctness, coverage or performance.
- Heavy preflight remains red for protected `zcode-vllm-tp2-v4`; no native
  PyPTO action started and no protected process was signalled.

## Resume action

Wait for the protected lane to exit naturally and rerun
`python tools/preflight.py --mode heavy`. If green, execute the frozen
single-DSO acceptance runbook. Only after single-DSO, TargetInfo,
CompileRequest/current-stream artifact and operator-executable gates may the
real Inductor scheduling/wrapper/registry transaction begin.
