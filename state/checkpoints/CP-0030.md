# CP-0030: Accepted immutable NVIDIA Artifact v1

- TensorIR `1c701ec6f7e7c547f6af02862603981f64e01091` extends its
  runtime-free device-code validator from structural Cubin checks to the exact
  declared CUDA entry and flattened parameter ABI. Follow-up
  `b25081afbeb53c9a882c68b440d06baa9e0f6b31` also requires the executable and
  constant sections to use coherent non-empty PT_LOAD file/VA mappings.
- The strict CUDA 13.3 path validates bounded ELF tables/segments/sections,
  numeric SM120, one canonical `STO_CUDA_ENTRY`, text/symbol linkage,
  KPARAM ordinals and widths, PARAM_CBANK section symbol/base/extent,
  CBANK_PARAM_SIZE, `.nv.constant0.<entry>` and load permissions. It performs
  no CUDA call, device query, module load, filesystem access or subprocess.
- PyPTO `9894f5babdca17d27de7b89540e28fc5c3b3e199` adds immutable,
  pointer-free Artifact v1; follow-up
  `4a82f2e40ce518d16fcbeec647061649564c42af` pins the final loader-safe
  validator and test fixtures. The full Artifact uses bounded canonical
  MessagePack and owns its Cubin bytes.
- Artifact v1 binds CompileRequest, KernelBuildSpec, source/specialization/
  callable/result/mutation identities, exact clean PyPTO/TensorIR/CUDA Tile/
  LLVM/toolkit/tileiras provenance, semantic route, producer options, target,
  grid, packing, workspace, launch and loader ABI. Schedule tile sizes and
  `uniform_signature=false` are cross-checked against the reconstructed ABI.
- The structured pipeline is specified by
  `src/compiler/nvidia/structured_tensorir_explicit_v1.json`; CMake verifies
  its Git-blob identity `a5398054a515c632506c3f1ffa3a0b5d9e819e83` and a
  compile-time assertion binds the same identity into Artifact.
- Configure-time and always-run build-time guards reject dirty/stale PyPTO,
  TensorIR, CUDA Tile or LLVM source provenance. Callers cannot forge the
  compiled build anchor: `NvidiaBackendBuildInfo` construction is private and
  Artifact obtains it internally.
- Final clean backend-ON CTest passes 8/8. The real producer seam compiles and
  validates TileIR plus static, dynamic 12-argument and scalar Cubins through
  pinned CUDA 13.3 `tileiras`; wrong entry, wrong layout and wrong scalar width
  are rejected. The exact source DSO Python compiler suite passes 128/128.
- The final ON DSO SHA-256 is
  `2b65764ea1fe8827deee303645dcab101a372b93cfebf0c1da4ff9f4585bcd06`.
  A fresh backend-OFF DSO SHA-256 is
  `82411883cc25467a3e5009d483ce177bab65c1d9f1242ce6a318316d6a63295a`;
  it passes 6/6, reports `compiled=false`, rejects strict Artifact creation and
  contains no private compiler dynamic dependency/export. Both products have
  no RPATH/RUNPATH and only five standard runtime dependencies.
- Independent architecture, C++ security, build/provenance and final source
  reviews report no remaining P0/P1. EV-0043 binds exact runs and hashes.
- TensorIR `b25081a...` is committed and pinned locally but is not published to
  the configured NVIDIA remote; external/fresh-clone reproducibility remains a
  separate publication action.
- The root control regression passes 198 tests plus 98 subtests beside the
  protected CPU-only lane with no NVIDIA compute PID, pause or external signal.
- This checkpoint does not accept a strict producer bridge. No compiler API
  yet consumes canonical source together with CompileRequest/KernelBuildSpec
  and returns this Artifact. ArtifactCache, subprocess compilation, CUDA
  module/context/current-stream execution, operators, frameworks, Qwen
  correctness/coverage and performance remain open.

## Resume action

Implement the narrow strict TensorIR producer bridge as a separate PyPTO
commit. It must consume canonical source, CompileRequest and KernelBuildSpec,
map the complete accepted schedule/options, forbid ambient overrides/fallback,
deep-copy the private TensorIR result into standard-only PyPTO values and call
Artifact v1. Keep cache and every CUDA runtime handle out of that transaction.
