# CP-0021: Accepted single-DSO compiler object boundary

- User resume ended the prior blocked interval. Multiple fresh heavy preflights
  returned zero long enough to complete every single-DSO acceptance gate.
- PyPTO commit `e463bce7849b2239d0457dcae78ccf41c65ffa55` contains exactly
  `CMakeLists.txt` and `python/bindings/CMakeLists.txt`: compiler sources build
  once in `pypto_compiler_objects`, while the sole nanobind module embeds that
  object target and inherits its public runtime ABI include.
- Fresh native build completed 239/239 edges; CTest passes 1/1. The corrected
  compile database has 228 unique production native rows, one intentional C++
  test duplicate, and 12 binding rows owned by `pypto_core`.
- Editable full JUnit is `(10235 tests, 0 failures, 0 errors, 59 skipped)`, or
  10,176 passed/59 skipped.
- Fresh wheel SHA-256 is
  `bd6d24c9857a409df9d48c604bd329d10808cde354803ee10765680d252f1da1`.
  Its 174 members are unique and path-safe; it contains exactly one native DSO.
- The build and clean-installed DSO bytes match at SHA-256
  `4974b4ecc95812833f6be723789f44bdac5463156a4fa066279e4866c0eb4f35`.
  `DT_NEEDED` contains only libstdc++, libm, libgcc_s, libc and the ELF loader;
  there is no PyPTO/TensorIR/CUDA Tile/AMD external DSO or RPATH/RUNPATH.
- Real probe-venv imports and `pypto-ir-trace` belong to that exact wheel.
  Wheel JUnit is `(10235, 0, 0, 57)`, or 10,178 passed/57 skipped; the two
  editable-only skips are precisely the installed console-script and symlink
  caller cases. The independent symlink probe passes.
- The first native and wheel blocks produced valid fresh artifacts but exited
  nonzero because old audit code misclassified one intentional test compile and
  matched the workspace name `tensor-ir` in an `ldd` path. Corrected recovery
  audits on the unchanged artifacts return zero; this lineage is explicit in
  EV-0033 and the runbook.
- Three independent final reviews report no remaining P0/P1 and approve the
  exact two-file commit.
- This accepts build/package ownership only. It does not accept TargetInfo,
  TensorIR composition, CUDA compilation/launch, operators, models, coverage or
  performance.
- A later pre-TargetInfo check observed a newly launched protected zcode/SGLang
  lane; no TargetInfo action began and no external process was signalled.

## Resume action

When a fresh `python tools/preflight.py --mode heavy` returns zero, execute
`docs/target_info_acceptance_gate.md` against candidate `9939b88`. Do not reuse
the single-DSO wheel and do not begin CompileRequest before TargetInfo accepts.
