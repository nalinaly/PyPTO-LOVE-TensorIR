# CP-0016: Source-only operator benchmark evidence contract v1

- `pypto-kernels` is clean at
  `6f73857b52c86dd98e25d23fbc825a675fb25b6a`.
- A framework/model-neutral benchmark v1 contract now covers every required
  operator report field without publishing any benchmark result.
- Candidate and baseline are symmetric measurements with independent
  correctness, environment, compile, artifact composition and profile traces.
  Multi-kernel/multi-provider SGLang baselines do not need a fake composite
  provider.
- Static configs/descriptors and dynamic workload/reset/metadata capacities are
  fail-closed. Matmul logical bytes/FLOPS are derived; attention/GDN values stay
  explicitly unavailable until reviewed formula registries exist.
- Every ABI argument has before/after digest evidence: read-only values must be
  unchanged and writable values must match correctness output/state digests.
- Strict canonical JSON parsing rejects duplicates, non-finite values,
  unknown/missing fields, noncanonical bytes and all stored derived-field or
  identity tampering.
- There is no publisher and no generated JSON. Tuning-database promotion is
  hard-coded false until driver and complete TargetInfo identity enter the
  reuse key. The contract therefore makes no performance claim.
- The full standalone suite passes 117 tests plus 71 subtests. A fresh wheel
  and clean plugin identity probe pass. Three final reviews report no P0/P1.
- The last heavy preflight remains red for the protected zcode TP=2 vLLM/gem5
  lane; no native PyPTO action has started.

## Resume action

Rerun heavy preflight. If the protected lane has exited naturally, execute
`docs/single_dso_acceptance_gate.md`. Otherwise continue light work only. Do
not implement benchmark publication until real PyPTO CUBIN and TargetInfo
identity exist.
