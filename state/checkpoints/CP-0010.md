# CP-0010: Unified GDN core ABI v1

- `pypto-kernels` is clean at
  `3c220c50532e98f6317f604199a5969f05f0f258`.
- Decode and variable-length prefill/extend share one 12-argument GDN core ABI.
  The boundary starts after projection/packing, owns depthwise causal conv plus
  FP32 recurrent update, and ends before Z-gated RMSNorm/output projection.
- ABI v1 freezes no-bias SiLU conv, channel order/weight order, Q/K
  normalization and scale, value-to-key head grouping, FP32 decay/update gates,
  the gated-delta formula, FP32 state layout and zero inactive output.
- Packed inputs and both state pools may use outer-pitched views; actual tensor
  and workspace intervals are pairwise checked in unsigned 64-bit address
  space. Width one has a canonical empty conv state and may use a zero pointer.
- Metadata validation covers packed variable lengths, decode single-token
  segments, unique physical slots and canonical inactive tails. It does not
  prove device contents or slot lifecycle.
- Conv/recurrent views are one paired generation under an exclusive lease from
  state preparation through stream completion. State zero/copy and segmented
  Radix checkpointing remain future PyPTO generic/runtime work.
- 82 tests plus 61 subtests, 1,000 randomized valid metadata cases, compileall,
  wheel build and clean import pass. Three independent reviews report no P0/P1.
- No GDN numerical reference, state copy, CUDA kernel, graph, integration or
  performance result is claimed.
- Heavy work remains paused for the protected zcode TP2 vLLM/gem5 lane; no
  external process was signalled.

## Resume action

Implement the deterministic GDN numerical reference next, including one-shot
prefill, segmented prefill and token-by-token decode state equivalence. Do not
start CUDA compilation until the heavy safety gate and PyPTO object-boundary
gate are green.
