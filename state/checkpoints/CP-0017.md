# CP-0017: StateBundle ownership and SGLang lifecycle inventory

- Exact state zero/copy/checkpoint is frozen as generic PyPTO runtime
  infrastructure. It is not a GDN operator, artifact or tuning record.
- `docs/state_bundle_transfer_design.md` specifies pointer-free bundle layout,
  zero/copy mapping, generation/lease law, current-stream ordering, alias rules,
  runtime strategy and required gates. It is documentation only.
- `pypto-framework-plugins` is clean at
  `0dfa2fc5278fc793b7eb746eee23d12451017823`.
- A source-hashed/AST-checked inventory locks 11 pinned SGLang sources and 35
  lifecycle sites. It proves the active registry→UnifiedRadixCache→MambaComponent
  route, while labeling legacy MambaRadixCache unselected.
- The inventory covers no/extra/lazy buffer, unified/page-major slot
  translation, overlap-safe deferred clear/COW, prefill/decode tracking,
  ping-pong donation, checkpoint commit and slot reuse.
- Immutable scope has `registration_ready=false` and orders single-DSO,
  TargetInfo, CompileRequest/current stream, generic StateBundle runtime, then
  adapter registration. Unsupported modes remain fail-closed.
- Plugin tests pass 122; wheel and clean kernels/plugin probe pass. Three final
  reviews report no P0/P1.
- No StateBundle API/executor or framework adapter exists. This checkpoint does
  not claim CUDA, graph, correctness, coverage or performance.
- Heavy preflight remains red for the protected zcode lane; no native PyPTO
  action has started.

## Resume action

Rerun heavy preflight. When green, finish the single-DSO acceptance runbook;
then TargetInfo and current-stream executable work must precede StateBundle
implementation. Continue light work only while the protected lane is active.
