# CP-0047: Fused-pointwise SM120 numerical correctness v2

- The user-authorized 22 GiB protected-lane admission is implemented as a new
  additive policy-2 family; every accepted CP46 v1 byte remains unchanged.
  Implementation commits `0b292da`, `339ff35`, `f0ea59a` and test-only
  `6cf5f95` end at tree `75b5e042...041bb`.
- Manifest-only commit `f6a064b73750a71bdd35b97420bd42ca6d425245`
  publishes exactly one canonical 1,553-byte file, SHA
  `d3b16079...d467036`. It binds the six v2 controls and complete accepted v1
  control/compiler-anchor identity.
- Independent premanifest review first rejected incomplete finalizer joins.
  Separate commits restore exact numerical replay hashes, process/preflight/
  runtime schemas, compiler lineage, run/child identities, symlink defense and
  adversarial controller coverage. Final pre/post-manifest reviews are GO with
  P0/P1/P2 zero.
- Post-manifest focused tests pass 58 methods. Isolated CUDA-hidden run
  `pypto-20260826T072707Z-1448115-216ff1` passes 304 tests plus 189 subtests,
  exits cleanly and observes no NVIDIA compute process.
- Real SM120 run `pypto-20260826T073309Z-1451510-e48ced` passes both parent
  preflights, the independent child gate and all periodic/post-exit isolation
  checks. Its process, initial/action preflight, gate, barrier and provisional
  SHAs are bound by EV-0060; no external or protected NVIDIA activity occurs.
- The eighteen fresh executable lifetimes cover the fixed nine FP32/BF16
  arithmetic, exp, reciprocal, rsqrt and maximum-boundary cases. Every input
  and canary is unchanged, candidate versus Torch is zero ULP, independent CPU
  joins are at most one ULP, special signs/classes match, every packet is
  released and every executable unloads terminally.
- The CPU-only finalizer publishes the 233,749-byte mode-`0444` report
  `reports/data/pypto-fused-pointwise-sm120-v2-pypto-20260826T073309Z-1451510-e48ced.json`,
  SHA `d4ffafc0...2eeedf0`. An exact retry exits 1 without replacing or changing
  the report. Two independent finalized-evidence audits return GO with
  P0/P1/P2 zero.
- EV-0060 accepts only the frozen nine-case real-SM120 correctness matrix. It
  does not accept general fused-pointwise behavior, performance, CUDA Graph,
  reduction, matmul, framework/model correctness or strict coverage.

## Resume action

Advance the reviewed RowReductionV3 head `17b2b3c` through fresh backend-OFF
and external backend-ON builds, native/Python regression, TensorIR/CUDA Tile
source and Cubin evidence, then add a separately controlled real-SM120
numerical gate. After reduction acceptance, repeat the compiler/Cubin and
runtime ladder for StructuredMatmulV4 `d755117` before beginning the
TorchInductor PyPTO backend implementation.
