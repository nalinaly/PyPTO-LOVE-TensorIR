# CP-0015: Independent Torch CPU operator cross-checks

- `pypto-kernels` is clean at
  `6afb3505d5785b20014c9865804cd7f7c49273fa`.
- Matmul, paged attention and GDN scalar references are now independently
  cross-checked against CPU Torch expressions without adding a production
  framework dependency.
- Torch imports only inside child processes with CUDA, HIP and ROCr visibility
  empty. The parent test process retains the framework-free import proof.
- Matmul covers 21 transpose/phase/rank/batch cases with bit-exact BF16 output.
- Attention covers no-prefix prefill, packed cached extend and a real
  shared-read-only-prefix PREFILL→DECODE path with private tails. Output and
  append-complete caches compare bit-exact in BF16.
- GDN uses structurally independent vectorized Torch primitives rather than a
  line-by-line translation. Width-4 conv, head grouping, FP32 recurrence,
  prefill→decode state continuity and slot reorder pass; BF16 output/conv state
  are exact and FP32 state uses an evidence-tight bound.
- The full standalone suite passes 105 tests plus 65 subtests. Reviews found
  and closed initial independence/coverage/tolerance P1 gaps; final review has
  no P0/P1.
- This is CPU semantic evidence only, not CUDA/SM120 correctness, framework
  integration, performance, model correctness or coverage.
- The protected zcode TP=2 vLLM/gem5 lane remains active. Heavy preflight is
  still red and no PyPTO native action has started.

## Resume action

Continue light work only. After the protected lane exits naturally, rerun the
heavy preflight and execute `docs/single_dso_acceptance_gate.md`. Commit only
the two PyPTO CMake files if every full-suite/fresh-wheel gate passes.
