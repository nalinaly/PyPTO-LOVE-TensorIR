# CP-0046: Fused-pointwise SM120 correctness controls v1

- Implementation `c98f984ddc4df7cd3354f5fbddadb12df072ed48`, tree
  `38b991779d84020f3bbe48944c98df604cf92c2c`, adds exactly nine new control,
  anchor, documentation and test files without changing CP43/44/v4 controls or
  isolation primitives.
- Manifest-only commit `438c25f5db0b3e40c79604352df81e536dcdf137`,
  tree `55f52ab550bdeff89c3a08dec19b52a848624f8c`, adds the canonical
  2,193-byte control manifest, SHA `ce20dd3a...7a6896`.
- The fixed matrix has nine cases and eighteen fresh executable lifetimes:
  FP32/BF16 arithmetic, exp, reciprocal and rsqrt plus the FP32
  16-input/64-assignment boundary. It covers rank-1/2/3 tails, 1/4/16-input
  ABIs, signed-zero FMA discrimination, special values, exact and ULP policies,
  direct Torch/CPU/candidate joins and 16-element prefix/suffix canaries.
- Final CUDA-hidden compiler-anchor runs `042728` and `042750` retain exact
  HIR, canonical TensorIR source, BuildSpec, Artifact and Cubin replay files for
  every case. Their normalized case records are byte-identical, SHA
  `01e8f99d...b844`, and Torch CUDA remains uninitialized.
- Exact-source module loading bypasses bytecode caches; matching cache entries
  are rejected before dependency loading. Seven development pycs were moved
  recoverably under the ignored workspace cache and no live matching pyc
  remains.
- The CPU-only finalizer reconstructs independent references, validates all
  three comparison relationships, complete process/preflight/gate/barrier/
  audit/replay joins, mode-0444 publication and immutable no-replace retry.
- Focused post-manifest run `044207` passes 32 tests plus 24 subtests. Full
  post-manifest run `044227` passes 278 tests plus 150 subtests. Both are
  CUDA-hidden with no compute PID, waiver, framework launch or external signal.
- Independent source, numerical and manifest reviews return GO with P0/P1/P2
  zero. EV-0059 binds exact files, anchors, runs, tests and review hashes.
- CP-0046 accepts only the control/finalizer and CPU anchor boundary. It does
  not accept a real GPU launch, provisional/final GPU report, general
  FusedPointwiseV2 correctness, performance, CUDA Graph, framework, model or
  strict-coverage result.

## Resume action

Run the reviewed controller only through `gpu-smoke` admission. Protected CPU
coexistence is allowed only when every protected NVIDIA mapping/compute check is
zero; otherwise wait without signalling or cleanup. On child success, run the
CPU-only finalizer, require a second no-replace finalization to fail without
changing the report, then obtain independent evidence review before accepting
fused-pointwise numerical correctness.
