# CP-0022: Accepted immutable SM120 TargetInfo and exact-toolchain gates

- PyPTO `042878dd6825bb65ed03f22db7b067fb96277623` integrates the reviewed
  34-path TargetInfo candidate on top of the accepted single-DSO boundary.
- Fresh native build completes 245/245 edges and CTest passes 2/2. The compile
  database assigns `target_info.cpp` once to the compiler object library and
  once to its C++ test; `compiler.cpp` belongs once to `pypto_core`.
- Fresh wheel SHA-256 is
  `dd614907ed7055b71e554ca869acb350773a856e8b10cc144dfc1420b179aaeb`.
  Its 177 members are unique/path-safe, it has one DSO, and build/install DSO
  bytes match at
  `dec8082f172908d0f7bbacc016c9a9685be2ab07df0f58352400068ab2f6c458`.
  Only five standard runtime `DT_NEEDED` entries remain and all resolve.
- Clean wheel TargetInfo JUnit is `(31,0,0,0)`; full JUnit is
  `(10266,0,0,57)`, or 10,209 passed/57 skipped. The 57-skip set is unchanged
  from the one-DSO wheel and the independent symlink case passes.
- EV-0034 binds Git source/tree, three successful run IDs, wheel/DSO, logs,
  JUnit, clean upstreams and a corrected read-only lineage recovery audit. It
  explicitly preserves the first recovery audit's copied tree-SHA typo.
- TargetInfo acceptance proves immutable SM120 identity, capability/resource
  policy, build/package/API and legacy Ascend isolation. It does not prove live
  CUDA device derivation, TensorIR/CUDA Tile composition, compile/load/launch,
  operators, models, coverage or performance.
- The inherited Triton remains rejected because its editable finder points
  outside the workspace. The root now has bounded dependency materialization,
  source-frozen review promotion, exact producer identity, offline build,
  complete wheel/native audit, pip-free fresh probe, reference-only SM120 smoke
  and reversible replacement tooling. These tools have unit evidence only:
  one of ten archives is pinned, no reviewed manifest/wheel/smoke/install exists.
- The tooling is committed in four ordered root transactions: `0c4cc34`,
  `befe44c`, `640c35a`, and `c987811`. The final isolated root suite passes 165
  tests plus 29 subtests; Python compilation, every runbook Bash fence and
  `git diff --check` pass. Three independent reviews report no remaining P0/P1.
- A shared/exclusive environment flock spans preflight through child reaping.
  Replacement is a direct-child, stdlib RECORD transaction with exact
  controller identity, durable pre-mutation journal, atomic no-replace backup
  publication, boundary audits and idempotent recovery/rollback.
- D-0010 keeps Triton reference-only and separates its LLVM from TensorIR.
  D-0011 records the user's explicit protected CPU-only coexistence policy.
  Default preflight remains fail-closed; GPU benchmarks never coexist; only a
  verified workspace run group can be paused/resumed/terminated.
- The first TargetInfo attempt stopped at 106/245 when memory crossed its safety
  floor and no external process was signalled. Its group was absent before an
  ownership-checked resume could signal it; a later namespace reset prevents
  attributing the disappearance. The replacement runner keeps ownership alive
  through pause/recovery. A later independent control run
  `pypto-20260824T075816Z-91897-64e1ea` passed beside seven active protected
  heavy processes with waiver=true, no protected NVIDIA PID, no pause/abort and
  no external signal. EV-0035 binds that live control evidence.

## Resume action

Execute `docs/triton_workspace_wheel_acceptance_gate.md` from materialization
through reviewed source locks, exact wheel/probe, exclusive reference-only GPU
smoke and reversible environment replacement. In parallel, the next isolated
PyPTO code transaction is the pointer-free CompileRequest data contract from
`docs/compile_request_artifact_design.md`. Do not begin ArtifactCache or claim
CUDA execution before CompileRequest/KernelBuildSpec and exact producer
identity gates accept.
