# CP-0043: Frontend vector-add SM120 smoke controls v1

- Root implementation commit `1d1fce4d63320beb3f29a265dd126891f37fb559`,
  tree `9abfbe232859db5aeca50ffd951009355c249fca`, adds a separately
  versioned correctness-only route for frontend-authored FP32 `[8,8]` and BF16
  `[128]` vector add. Manifest-only commit
  `47a0c15de510fbdea1eb029ff4e5f0cc9cdc5b77`, tree
  `88996b8352452c51d8f82c27fa9aa582a3c41fc1`, binds it without a
  self-referential hash cycle.
- Canonical manifest
  `state/contracts/pypto_frontend_vector_add_sm120_v1.json` is 1,776 bytes with
  SHA-256 `f16c4fba...d8eed`. It binds five new control blobs and the exact three
  unchanged v4 isolation primitives. The complete accepted v4 parent manifest
  remains pinned by SHA-256 `a079c4d2...98bf`.
- The fixed child constructs explicit `In/In` HIR, round-trips exact Program
  bytes, calls `pypto.compiler.compile_structured_strict` once per case, checks
  all CP-0041 source/projection/callable anchors and deterministic Cubin bytes,
  then creates two independent `NvidiaExecutable` lifetimes per Artifact.
  Launch packets, non-default current stream, capture-free state, external
  synchronization, input immutability, packet release and terminal unload are
  all evidence obligations.
- The CPU-only finalizer has no producer or NVIDIA runtime entry point. It
  deserializes the exact request, two HIR programs, two BuildSpecs and two
  Artifacts; rejects broader recursive claims and every unknown replay entry;
  joins complete process/preflight/gate/barrier/audit/control/runtime identity;
  and publishes read-only without replacement.
- The first source review correctly returned NO-GO for a real ArtifactTarget API
  error, weakened finalizer checks and insufficient tests. All findings were
  repaired before the final implementation/manifest commits. Final source and
  manifest reviews return GO with P0/P1/P2 = 0.
- Wrapper-owned CUDA-hidden exact-DSO run `142856` invokes the final runner's
  Artifact validation for both cases with CC120 and pinned Cubins. Run `143154`
  additionally serializes both cases and executes the final embedded replay
  child; it returns zero with both outer and replay Torch CUDA state
  uninitialized. Neither is a GPU correctness result.
- The new-family suite passes 21 tests plus 13 subtests. The complete
  clean root suite passes 246 tests plus 126 subtests at run `143449`.
- CP-0043 accepts only the reviewed fixed-command control/finalizer route and
  its CPU/compiler evidence. It does not accept a CUDA launch, frontend GPU
  numerical correctness, broader operator coverage, framework/model coverage,
  CUDA Graph or performance.

## Resume action

Run the fixed `gpu-smoke` controller only from a fresh green admission. Use the
explicit protected-zero-NVIDIA authorization only if a protected CPU lane is
present and both its NVIDIA runtime-map and compute-PID sets are empty. Preserve
any failed child/provisional as diagnostic evidence. On success, run the exact
CPU-only finalizer with the printed provisional SHA, independently review the
final report, and persist a separate runtime checkpoint before extending to
fused pointwise, row reduction or structured matmul.
