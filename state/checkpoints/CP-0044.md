# CP-0044: Frontend HIR vector-add real-SM120 correctness v1

- Finalized run `pypto-20260825T145519Z-1142938-70ac73` executes the exact
  CP-0043 child on the NVIDIA GeForce RTX 5090 Laptop GPU, SM120. Canonical final
  report
  `reports/data/pypto-frontend-vector-add-sm120-pypto-20260825T145519Z-1142938-70ac73.json`
  is 32,676 bytes, mode `0444`, with SHA-256 `8dbbfbf3...28e8`.
- The run starts from clean root `b8dae36`, PyPTO `642ff5b`, exact backend-ON
  DSO `4b796b1e...c6fb`, private TensorIR `1dcb38c`, CUDA Tile `af241704`,
  LLVM `57109bef` and the fixed v1 control manifest `f16c4fba...d8eed`.
- Two explicit-`In` HIR programs round-trip byte-exactly. The public
  `compile_structured_strict` facade is called exactly once for FP32 `[8,8]`
  and once for BF16 `[128]`. Both final BuildSpec/Artifact/Cubin identities
  match the CP-0041 projection and CP-0043 deterministic-Cubin anchors with no
  fallback.
- Each Artifact runs through two fresh `NvidiaExecutable` lifetimes on one
  selected non-default current stream. All four launches are capture-free,
  externally synchronized, byte-correct against CPU references, leave both
  inputs unchanged, release the packet after synchronization, explicitly
  unload and reach terminal `Unloaded` with zero bound context identity.
- BF16 reference semantics are BF16 inputs promoted to FP32, added in FP32 and
  rounded once to BF16. An independent CUDA-hidden CPU run reproduces all four
  input/output hashes.
- Parent preflight, release gate, start barrier, child gate, periodic watchdog
  and post-exit audit report no external/protected NVIDIA compute PID, no
  protected NVIDIA runtime mapping and no unreadable protected map. The child
  imports no Triton, SGLang or FlashInfer provider.
- The no-site finalizer reconstructs the request, both HIR programs, BuildSpecs
  and Artifacts through the exact DSO without compiling or initializing CUDA.
  A second finalization passes validation but fails only at no-replace
  publication; the original report remains byte- and metadata-identical.
- Independent evidence review reconstructs the entire report byte-for-byte and
  returns `GO_FRONTEND_SM120_VECTOR_ADD_V1`, P0/P1/P2 = 0.
- CP-0044 accepts only the two fixed frontend vector-add fixtures through
  `HIR -> compile_structured_strict -> BuildSpec/Artifact/Cubin ->
  NvidiaExecutable` on real SM120. It does not accept general vector-add/BF16
  behavior, fused pointwise, reduction, matmul, CUDA Graph, performance,
  TorchInductor, SGLang, Qwen or strict model coverage.

## Resume action

Generalize the compile-free emitter and structured identity preparation from
one hard-coded add form to a bounded generic pointwise chain while preserving
the accepted add source/identity/Cubin controls byte-for-byte. Land CPU/compiler
correctness first, then add a separately versioned fused-pointwise SM120
correctness gate. Row reduction and structured matmul follow as separate
operator-family checkpoints; framework integration remains later.
