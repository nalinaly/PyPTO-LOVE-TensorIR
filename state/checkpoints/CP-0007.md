# CP-0007: Operator provenance identity and collector map

- `pypto-kernels` is clean at
  `c50df674067e72df0c57fd6bee903414518a99dd`.
- Operator artifact provenance now has a complete canonical digest and accepts
  only explicit ABIs in the matmul, paged-attention and GDN catalog. Generic,
  TensorIR, model/layer names and unknown ABI identities fail closed.
- Physical compiled artifacts, paths, locking, cache and loading remain owned
  by `pypto`. No second global compiler cache was added to the operator library.
- 46 kernel-project tests plus 15 subtests, compileall, wheel build and clean
  wheel import pass. Three independent reviews report no P0/P1.
- The pinned Torch/SGLang runtime collector source map is frozen in
  `docs/coverage_collector_map.md`. The first implementation must be eager-only
  and development-only; until a complete drain protocol and artifact launch
  binding exist it must emit `closed_world=false` and strict audit must reject.
- No compiled kernel, collector run, model execution, correctness, coverage or
  performance result is claimed.

## Resume action

Keep the heavy gate in force while the protected zcode lane is active. Finish
the PyPTO single-DSO full-suite/fresh-wheel gate before applying the reviewed
SM120 TargetInfo transaction. The next operator-side schema task may define
paged-attention ABI v1, but it must stay framework-neutral and use pinned
SGLang metadata only as an adapter source map.
