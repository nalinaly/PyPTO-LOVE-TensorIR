# CP-0037: Canonical dtype finalizer repair and SM120 smoke manifest v4

- V3 run `pypto-20260825T073624Z-900485-7df250` passed fresh GPU-smoke
  admission on the RTX 5090, used root `800a766` and PyPTO `206447c`, and exited
  `rc=0`. Its child published a 28,414-byte provisional document with SHA-256
  `64c0906b7fe57bdddf4c26f7b205a51918a91b93284604f363633ef439d34cfe`.
- The provisional records two complete lifetimes for each static, dynamic and
  scalar case: six non-default-current-stream launches, external
  synchronization, logical-output hashes equal to their references, Torch
  equality, unchanged inputs/padding, explicit unload and terminal `Unloaded`.
  It records no fallback or forbidden provider import. These are retained child
  records, not accepted GPU correctness, because finalization did not complete.
- Pre-release, last and post-exit external/protected NVIDIA compute,
  protected-runtime-mapping and unreadable-map sets are empty. The owned PGID
  `900588` has no survivor and no external signal evidence exists.
- The required no-site v3 finalizer failed closed with session-observed error
  `FinalizeError: live PyPTO runtime observation differs`; no final report was
  published. Its stderr was not persisted as a finalizer sidecar. Inspection
  isolated the mismatch to `supported_compute_dtypes`: the real producer wrote
  canonical `["FP32", "BF16"]`, while the v3 finalizer fixture expected the
  handwritten order `["BF16", "FP32"]`.
- The producer is authoritative. `NvidiaTargetInfo` sorts stable DataType codes;
  FP32 is `0x34` and BF16 is `0x40`. The runner and independent serialized
  TargetInfo replay preserve FP32 then BF16. The defect is in the root
  finalizer/control fixture, not PyPTO, TensorIR, the DSO, Artifact or Cubin.
- Root Layer A4 `5564008fddeaaf0a9861ee5c38c895558f577600`, tree
  `b1676a118604c22eebaec787987806f3cf1aeebb`, introduces one shared ordered
  contract tuple and exact comparison. It deliberately does not sort or
  set-normalize evidence. Tests reject reversed, missing, duplicate, extra,
  unknown, numeric and wrong-container forms, and semantic replay independently
  rejects dtype-order drift.
- Manifest-only Layer B4 `7639d820f4d74972b493c01adc69c92087eefdea`,
  tree `52e37ab60276ebec2e06b46a4b55c39af4c22d62`, adds
  `state/contracts/pypto_nvidia_executable_sm120_v4.json`, 1,569 bytes, SHA-256
  `a079c4d252aa346bb19a64a6ad3947867b76e7c778f7234125078fb16b2598bf`.
  Control manifests v1 through v3 remain immutable.
- Finalizer-focused run `pypto-20260825T074328Z-903562-ee6d75` passes 20 tests
  plus 15 subtests. Clean post-v4 root run
  `pypto-20260825T074420Z-903996-c8d50d` passes 225 tests plus 113 subtests;
  its JUnit SHA-256 is
  `9f1d81ed44b9b9b959656d2eff8049b4c529ae8b89fe61c05a8f082783fb1acd`.
- A corrected v4 finalizer cannot promote the v3 provisional: the finalizer,
  contract and control-manifest bytes differ, and exact child/control joins are
  mandatory. The v3 run remains an unfinalized diagnostic. A fresh clean v4
  GPU-smoke plus v4 CPU finalizer is required.
- CP-0037 records but does not accept the v3 child/provisional and finalizer
  failure. It accepts only the generic canonical dtype-order contract, v4
  finalizer/control implementation and supporting CPU evidence. It does not
  accept finalized GPU correctness, CUDA Graph, frontend lowering, operator
  kernels, TorchInductor/SGLang registration, Qwen correctness, strict coverage,
  profiling or performance.

## Resume action

After independent review and a clean CP-0037 commit, run the exact v4 command
from `docs/pypto_nvidia_executable_sm120_smoke.md` under a fresh green
`gpu-smoke` admission. Finalize only the new v4 provisional with its printed
SHA. Preserve v3 as diagnostic evidence and do not attempt cross-version
promotion. Never signal protected amdgpu-sim/zcode processes.
