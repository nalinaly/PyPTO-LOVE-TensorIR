# CP-0013: Structured matmul numerical reference

- `pypto-kernels` is clean at
  `2787e1efbb769c8c86eb92218291ba936903ded4`.
- Matmul ABI v1 now has a deterministic standard-library reference for
  NN/NT/TN/TT, rank-2 and explicit batched rank-3 including batch size one.
- Inputs and output round through BF16; each K-order multiply/add rounds through
  FP32. Inputs are snapshotted once and remain unmodified; output is immutable.
- 97 tests plus 65 subtests, compileall, wheel build and clean import pass.
  Three independent reviews report no P0/P1.
- This is an oracle for future TensorIR/CUDA Tile tests. It has not run through
  TensorIR or CUDA, and future tiled reductions must compare with tolerance
  rather than assume identical accumulation order.
- Heavy native/model work remains paused for protected zcode; no external
  process was signalled.

## Resume action

Keep this reference alongside the matmul ABI when the TensorIR bridge lands.
First compare small-M and large-M generated kernels for correctness, then add
microbenchmarks and tuning records; do not use cuBLAS in strict results.
