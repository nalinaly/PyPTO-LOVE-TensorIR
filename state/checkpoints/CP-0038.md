# CP-0038: Finalized real SM120 NvidiaExecutable correctness v1

- Clean v4 run `pypto-20260825T080254Z-910620-c669d9` used root `37c16b3`,
  PyPTO `206447c`, private TensorIR `1dcb38c`, CUDA Tile `af24170`, manifest v4
  and the exact backend-ON DSO. Admission observed the RTX 5090/SM120, 20,980
  MiB free GPU memory, no external/protected NVIDIA compute or protected runtime
  mapping, and no unreadable protected map.
- The child exited zero and published provisional SHA-256
  `954a266ed5d592698649ad1947fe1879e75dce2f3d9ebad9e16c74034076221f`.
  It completed two lifetimes each for static FP32 add, dynamic-stride FP32 add,
  and FP16 tensor plus by-value FP32 scalar.
- All six executions used the caller's non-default current stream and explicit
  external synchronization. Expected and actual logical-output hashes match;
  Torch equality, input preservation and dynamic padding preservation hold.
  Packets remain alive through synchronization. Every module is explicitly
  unloaded and reaches terminal `Unloaded` with no bound context retained.
- The v4 no-site finalizer succeeded and published the immutable 29,107-byte
  final report
  `reports/data/pypto-nvidia-executable-sm120-pypto-20260825T080254Z-910620-c669d9.json`
  with SHA-256
  `727362d7879d58cbee07b11050b17ad149274e8087b0d1872b8f186a66a272a9`
  and status `accepted-real-sm120-correctness-smoke`.
- Finalization joins root/control identity, all seven v4 blobs, process,
  preflight, release gate, start barrier and provisional bytes. An independent
  no-site replay deserializes the CompileRequest, three BuildSpecs and three
  Artifacts, verifies exact Cubin size/SHA and ABI identities, and proves live
  runtime TargetInfo equals serialized TargetInfo. All three Artifacts record
  `fallback_used=false`; no Triton, SGLang or FlashInfer provider was imported.
- The accepted provider is narrowly `pypto.tensorir` and the runtime object is
  `NvidiaExecutable`. This closes the minimal compiler-produced Cubin through
  real libcuda current-stream launch/unload gate on RTX 5090. It is not an
  Inductor, SGLang, custom operator, model, CUDA Graph, coverage or performance
  result.
- Safety sidecars are retained at exact hashes in EV-0051. The owned PGID
  `910707` has no survivor; no external process was signalled. The earlier v3
  provisional/finalizer failure remains diagnostic history and is not reused.
- CP-0038 accepts only the minimal real-SM120 `NvidiaExecutable` correctness v1
  scope above. Frontend HIR lowering and standalone operator execution remain
  mandatory next gates before any framework or Qwen milestone.

## Resume action

Preserve the finalized report and v4 controls. Do not rerun this smoke merely
to change evidence. Advance the standalone PyPTO path to frontend HIR -> private
TensorIR -> CUDA Tile -> Artifact -> `NvidiaExecutable`, in this order:

1. vector add;
2. fused pointwise chain;
3. row reduction;
4. simple structured matmul.

Each case requires a CPU/reference oracle, exact Artifact provenance and a real
SM120 correctness run. Performance, operators, Inductor and SGLang remain after
this frontend gate. Never signal protected amdgpu-sim/zcode processes.
