# CP-0006: Strict coverage evidence contract

- `pypto-framework-plugins` is clean at
  `d46a58c28a14526d2a00155ad95f3985e18295f1`.
- Its framework-neutral auditor now binds a normalized activity trace to a
  fixed collector protocol revision and a digest-bound PyPTO artifact registry.
  Strict policy is non-vacuous and exact over calls and GPU time.
- Model-forward compute cannot be hidden as runtime or an unknown sampling
  provider. Strict violations are latched, write a durable partial report
  before raising, and cannot later be converted into success.
- Report paths are absolute before locking, held under a lifetime process lock,
  reject existing evidence, and allow only the owning instance to advance its
  exact partial bytes to final.
- 68 plugin tests, compileall, and a clean wheel build pass. The wheel SHA-256
  is `3b9e9331fe7df25511699fe0b08b58271510c90d25fc2ee92e7ab840c2d9e947`.
  Two independent reviews report no remaining P0/P1.
- This is an evidence contract only. No CUDA collector, Qwen runtime trace,
  model correctness, strict coverage percentage, or performance result exists.
- Protected zcode TP2 vLLM/gem5 work was active during this checkpoint. Only
  light source/tests ran; no external process was signalled or cleaned up.

## Resume action

Keep heavy work gated by `tools/preflight.py --mode heavy`. Once green, finish
the isolated PyPTO single-DSO full-suite/fresh-wheel gate and commit its two
staged CMake files. Then add the reviewed immutable NVIDIA SM120 target identity
as a separate commit; do not mix target semantics into the object-boundary
transaction.
