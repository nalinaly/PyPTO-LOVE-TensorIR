[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "pypto-kernels"
version = "0.1.0"
description = "Native PyPTO tile operators required by Qwen3.5 inference"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
